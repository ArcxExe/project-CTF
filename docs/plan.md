# Детальный план реализации проекта (CTF Platform & Academic Grading)

Настоящий документ описывает пошаговый статус и план разработки платформы с разделением на 12 этапов. Архитектура проекта строго разделяет слои (`Controller` -> `Service` -> `Repository`), использует конструкторы для внедрения зависимостей, а DTO контракты исключают передачу JPA-сущностей клиенту.

---

## Статус выполнения модулей

- **[x] Этап 1: Подготовка инфраструктуры, БД и миграций Flyway** (Скрипт `V8__academic_and_grading.sql` написан и применен).
- **[x] Этап 2: Базовая аутентификация, Security и Регистрация со связыванием** (Реализована проверка `studentCode` при регистрации, логика привязки `User` <-> `Student` и ручное одобрение/отклонение ADMIN).
- **[x] Этап 3: Академическая структура и импорт студентов** (Реализованы JPA-сущности, репозитории, CSV-импорт и `AdminStudentController` на бэкенде).
- **[x] Этап 4: Модуль учета лабораторных работ и расчет $v_1$** (Запланировано).
- **[ ] Этап 5: Модуль тестового этапа (Квизы)** (Запланировано).
- **[ ] Этап 6: Модуль задач CTF, First Blood и Задачи с файлом** (Запланировано).
- **[x] Этап 7: Модуль игровых промокодов** (Запланировано).
- **[x] Этап 8: Административные корректировки баллов** (Запланировано).
- **[ ] Этап 9: Итоговые показатели ($v_2, S$) и Шкала оценок** (Запланировано).
- **[ ] Этап 10: Логирование аудита (Audit Log)** (Запланировано).
- **[ ] Этап 11: Экспорт отчетов (CSV/Excel)** (Запланировано).
- **[/] Этап 12: Реактивный интерфейс, Интеграция, Тесты и Документация** (API-сервисы на фронтенде написаны **[x]**; Перенос UI в `AdminPages.tsx` — в процессе выполнения агентом **Jules** **[/]**).

---

## Подробное описание этапов

### Этап 1: Подготовка инфраструктуры, БД и миграций Flyway [x] Выполнено
- [x] Создана миграция `V8__academic_and_grading.sql` со всеми таблицами:
  - `academic_flows`, `academic_groups`, `students`, `lab_scores`, `lab_score_history`, `manual_submissions`, `promo_codes`, `grading_scales`, `audit_logs`.
- [x] Верифицирована накатка миграций Flyway на PostgreSQL.

### Этап 2: Базовая аутентификация, Security и Регистрация со связыванием [x] Выполнено
- [x] Модификация `RegisterRequest` и метода `AuthService.register()` для приема и верификации `studentCode`.
- [x] Эндпоинты в `AdminStudentController` для подтверждения/отклонения связей `User` <-> `Student`.
- [x] Блокировка сессий заблокированных (`BLOCKED`) и дисквалифицированных (`DISQUALIFIED`) студентов в фильтре безопасности.

### Этап 3: Академическая структура и импорт студентов [x] Выполнено
- [x] JPA-сущности `AcademicFlow`, `AcademicGroup`, `Student` и их репозитории.
- [x] `StudentImportService` для парсинга и сохранения студентов из CSV.
- [x] Контроллер `AdminStudentController` с эндпоинтами для импорта, фильтрации и пагинации, смены статуса студента.

### Этап 4: Модуль учета лабораторных работ и расчет $v_1$ [x] Запланировано
- [ ] JPA-сущности `LabScore` и `LabScoreHistory` с транзакционной логикой сохранения истории причин.
- [ ] Контроллер `AdminLabScoreController` для ручной корректировки и импорта баллов из CSV.
- [ ] Метод расчета $M_{max}$ по совокупностям и расчет коэффициента $v_1 = M / M_{max}$.

### Этап 5: Модуль тестового этапа (Квизы) [ ] Запланировано
- [ ] Сущности `Quiz`, `QuizQuestion`, `QuizOption`, `QuizSubmission`.
- [ ] Поддержка типов вопросов `SINGLE`, `MULTIPLE`, `SEQUENCE` с валидацией на сервере.
- [ ] Автоматическая проверка и лимиты времени сессий квиза.

### Этап 6: Модуль задач CTF, First Blood и Задачи с файлом [ ] Запланировано
- [ ] Механика First Blood с пессимистическим локом `SELECT FOR UPDATE` записи задачи.
- [ ] Задачи с типом `FILE_UPLOAD` и сущность `ManualSubmission`.
- [ ] Расчет понижающих коэффициентов за первенство сдачи отчетов (100% / 50% / 10%) и переопределение баллов админом.

### Этап 7: Модуль игровых промокодов [x] Запланировано
- [ ] Сущность `PromoCode` и транзакционная активация с блокировкой `PESSIMISTIC_WRITE` строки кода.
- [ ] Поддержка эффектов (`FIXED_ADD`, `FIXED_SUB`, `DOUBLE_COEFF`) и кэширование временных коэффициентов в Redis.

### Этап 8: Административные корректировки баллов [x] Запланировано
- [ ] Сущность `ScoreAdjustment` и контроллер корректировок.
- [ ] Пересчет лидерборда и SSE-широковещание изменений.

### Этап 9: Итоговые показатели ($v_2, S$) и Шкала оценок [ ] Запланировано
- [ ] Расчет итогового балла студента за CTF ($N$) и $N_{max}$ по совокупности.
- [ ] Вычисление $v_2 = N / N_{max}$ и итогового коэффициента $S = (v_1 + v_2) / 2$.
- [ ] JPA-сущность `GradingScale` и маппинг $S$ в оценку на бэкенде.

### Этап 10: Логирование аудита (Audit Log) [ ] Запланировано
- [ ] Реализация аспектов AOP для аннотации `@AuditAction` с записью в `audit_logs`.
- [ ] Логирование авторизации, решений, промокодов, блокировок, изменения шкал и баллов.

### Этап 11: Экспорт отчетов (CSV/Excel) [ ] Запланировано
- [ ] Стриминг отчетов в `HttpServletResponse` через `ReportService`.
- [ ] Четыре регламентированных CSV-отчета для ADMIN.

### Этап 12: Реактивный интерфейс, Интеграция, Тесты и Документация [/] В процессе выполнения (Jules / API Готово)
- [x] Реализация API-клиентов на фронтенде в `/frontend/src/shared/api/services/`.
- [/] Замена mock-страниц на реальные React-компоненты в `AdminPages.tsx` (Выполняет Jules).
- [ ] Написание интеграционных тестов с Testcontainers (PostgreSQL) для проверки конкурентных локов и формул.
